import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UploadCard = () => {
  return (
    <View>
      <Text>UploadCard</Text>
    </View>
  );
};

export default UploadCard;

const styles = StyleSheet.create({});
